# Jenkins Configuration
# Get this secret from Jenkins UI after creating the docker-agent node
# Go to: Manage Jenkins → Manage Nodes and Clouds → docker-agent → Agent secret
JENKINS_SECRET=your_jenkins_agent_secret_here

# WireGuard VPN Configuration
# Set to 'auto' to auto-detect your VPS public IP, or specify your VPS IP address
WIREGUARD_SERVER_URL=auto

# Number of WireGuard client configurations to generate
# Each peer gets a separate config file (peer1, peer2, etc.)
WIREGUARD_PEERS=1

